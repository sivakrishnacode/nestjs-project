import { AnotherMiMiddleware } from './another-mi.middleware';

describe('AnotherMiMiddleware', () => {
  it('should be defined', () => {
    expect(new AnotherMiMiddleware()).toBeDefined();
  });
});
